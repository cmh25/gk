\e 0
/ power: negative base with a non-whole exponent -> domain (no complex numbers)
.[^;(-2;0.5);:]
.[^;(-2 3;0.5);:]
/ power: whole exponents and NaN base must still compute (not domain)
(-2)^2
(-2)^2.0
(-2)^3
0n^0.5
2^10
4^0.5
/ $ cast: unrecognized symbol specifier -> domain
.[$;(`zzz;5);:]
/ $ cast: empty-symbol cast-to-symbol and width-pad must still work
`$"abc"
3$"ab"
\\
