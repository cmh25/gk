/ UAF regression: an amend function re-reads, through the namespace, a sibling
/ value being rewritten by the same in-place whole-dict amend loop. The global
/ name cache held a borrowed dict-slot pointer that the in-place store freed;
/ amend_fe now clears the cache so the next lookup can't deref freed memory.
.[`;`k`a`b;:;5]
.[`.k;`d`e;:;9]
@[`.k;`@`x`u;{d}]
.k
\\
